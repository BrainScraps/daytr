.row-fluid
  .span12
    %h3 Good morning Free form.

    = form_for @user, :html => {:id => "signup_free"} do |f|
      .field
        = f.label :email
        = f.text_field :email
      .field
        = f.label :password
        = f.password_field :password
      .field
        = f.label :password_confirmation
        = f.password_field :password_confirmation
      .actions
        = f.submit "Create Account"